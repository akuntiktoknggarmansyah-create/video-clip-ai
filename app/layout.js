import './globals.css';

export const metadata = {
  title: 'ClipAI — Auto Clip Video YouTube Jadi Konten Viral',
  description: 'Ubah video YouTube panjang jadi klip pendek siap viral pakai AI (Gemini)',
};

export default function RootLayout({ children }) {
  return (
    <html lang="id">
      <body>{children}</body>
    </html>
  );
}
